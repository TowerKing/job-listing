
<div>
  <h2><%= @job.title %></h2>
  <p>
    职缺描述：<%= @job.description %> <br />
    薪资上限：<%= @job.wage_upper_bound %> <br />
    薪资下限：<%= @job.wage_lower_bound %> <br />
    联系方式：<%= @job.contact_email %>
  </p>
</div>

<div class="group">
  <%= link_to("投递履历", new_job_resume_path(@job, @resume), :class => "btn btn-primary pull-left") %>
</div>
